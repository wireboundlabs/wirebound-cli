# Wirebound CLI profile — copy to ~/.config/wirebound/profiles/<name>.env
#
#   cp docs/templates/profile.env.example ~/.config/wirebound/profiles/acme.env
#   chmod 600 ~/.config/wirebound/profiles/acme.env
#
# Then either:
#   export WIREBOUND_PROFILE=acme
# or pass --profile acme on each command.

# --- Auth0 (required for wirebound auth0:* commands) ---
# Domain from Auth0 Dashboard → Settings → Domain (no https://)
AUTH0_DOMAIN=your-tenant.us.auth0.com

# From Auth0 Dashboard → Applications → Your M2M app → Settings
AUTH0_MGMT_CLIENT_ID=
AUTH0_MGMT_CLIENT_SECRET=
